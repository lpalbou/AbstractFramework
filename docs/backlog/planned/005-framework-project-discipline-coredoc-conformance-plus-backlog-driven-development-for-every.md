# 005-framework: [TASK] Project discipline: coredoc conformance plus backlog-driven development for every package

> Package: framework
> Type: task
> Created: 2026-07-13 21:15:03 +0200
> Priority: normal
> Labels: wave-project-discipline, seat-continuum, seat-skill

## Summary
OPERATOR DIRECTIVE (laurent 21:12): each project carries the coredoc standard (core doc set, llms.txt/llms-full.txt faithful) and handles development through backlog items, using the abstractskill coredoc and backlog skills as the portable teachings executors activate. Continuum leads the board surface (per-package docs/backlog posture, conformance as derived read, remediation as work items); skill confirms/ports the two skills to the curated shelf; joins the workforce-console design at the skills-per-member lane. Receipts: hub c1654; wave-project-discipline. Labels: wave-project-discipline, seat-continuum, seat-skill.


Work-item id: abstractframework-0005

## Why

Operator directive (laurent 2026-07-13 21:12): each project carries the coredoc
standard and handles development through backlog items. Re-activated by the
operator 2026-07-21 00:46 ("how can this be a quiet hour ? look at the backlog"
— dm:framework--laurent#107): seats were reporting "no open claim, nothing held"
as a good state while repo backlogs held ~310 open items (planned+proposed).
"No open claim" with a non-empty backlog is the state the work-until-completion
rule forbids.

## Scope

### In scope

- Backlog-driven development enforced as a standing posture: every seat holds
  one live claim drawn from its own backlog, or a named gate on the record.
- Delegate dispatch + accountability loop: dispatch (commons 3815), roster of
  claims taken, renudge of non-responders, counts in the hourly table.
- Card hygiene for the framework root backlog itself (this card was a skeleton
  with empty Why/Scope — filled as part of the claim).
- Coordination with continuum (board surface: per-package backlog posture,
  conformance as derived read) and skill (coredoc + backlog skills on the
  curated shelf) per the original directive.

### Out of scope

- Doing other seats' backlog items for them (one seat = one package).
- Coredoc conformance remediation inside individual repos (each seat's own
  work items; this card tracks the wave, not the edits).

## Acceptance criteria

- [ ] Every active seat holds one live claim from its backlog OR a named gate
      on its claim row (roster evidence in the hourly table).
- [ ] framework root backlog triaged: stale/completed-in-substance cards moved
      out of planned/, skeletal cards filled or deprecated.
- [ ] continuum: board renders per-package backlog posture (their lead half).
- [ ] skill: coredoc + backlog skills confirmed on the curated shelf (their half).

## Receipts

- hub c1654 (original directive relay); dm:framework--laurent#107 (operator
  re-activation); commons 3815 (2026-07-21 00:49 work dispatch, ~310 open items
  counted per package); claim row claim:abstractframework-0005 (commons store).
