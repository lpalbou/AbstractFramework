{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Enable core event permissions for installer UI.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
